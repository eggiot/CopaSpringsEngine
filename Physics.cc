/*
 * Physics.cc
 *
 *  Created on: 22 Aug 2011
 *      Author: Eliot J. Walker
 */

// include dynamics library
//#include <Box2D/Box2D.h>

/*namespace Physics
{
class World
{
protected:
    b2Vec2 physics_gravity: 0.0f, 10.0f;
    b2World physics_world(physics_gravity, true);
public:
    World(float gravity, bool sleep);
    b2World& getWorld();
};
}

Physics::World::World(float gravity, bool sleep)
{
    physics_gravity = gravity;
    b2Vec2 gravity_vec(0.0f, gravity);
    b2World new_world(gravity_vec, &sleep);
    physics_world = new_world;
}
*/
